#!/usr/bin/env bash
echo "id $*" >> "${LTI_TEST_CAPTURE:-/dev/null}"
case "${1:-}" in
    -u)  printf '%s\n' "${LTI_MOCK_UID:-1000}" ;;
    -un) printf '%s\n' "${LTI_MOCK_USER:-tester}" ;;
    -nG) printf '%s\n' "${LTI_MOCK_GROUPS:-tester}" ;;
    *)   printf '%s\n' "${LTI_MOCK_UID:-1000}" ;;
esac
exit 0
